# Decision Engine

The engine ranks compatible render engines using weighted quality, identity preservation, speed, license confidence and cost. It never overrides a license gate; for global publication, low-confidence licenses are excluded by default.

Scores are heuristic starting points and are meant to be updated from the Experiment Registry after real Harum renders.