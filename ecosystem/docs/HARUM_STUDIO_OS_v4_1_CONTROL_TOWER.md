# Control Tower

Run:

```bash
python -c "from harumstudio.control_tower import serve; serve('runtime/studio.db')"
```

It serves a local browser dashboard with factory counts, jobs and events. It is intentionally dependency-free.