# Decision Engine

The router scores compatible engines by quality, identity preservation, speed, license confidence and cost. For globally published masters, low-confidence license routes are excluded by default.

Scores are starting heuristics, not permanent truths. The Experiment Registry is intended to replace hand-set assumptions with metrics from real Harum renders.
