[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name="harum-studio-os"
version="4.1.0"
requires-python=">=3.10"

[project.scripts]
harum-studio="harumstudio.cli:main"

[tool.setuptools.packages.find]
where=["."]
include=["harumstudio*"]
