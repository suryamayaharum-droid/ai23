# HARUM Cognitive Cascade v22

Resource-aware local intelligence cascade:

Qwen3.5-0.8B Q4_0 → deterministic judge → Qwen3-1.7B Q8_0 fallback → validated lesson memory.

The fast brain handles routine routing. Invalid or high-risk decisions escalate. Only outputs that pass deterministic gates enter the lesson store and can later be retrieved as few-shot context.

The LLM remains advisory. Identity, capabilities, allowlists, signatures, tests and other deterministic gates remain authoritative.