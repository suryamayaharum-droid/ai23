# HARUM Crystal Cortex Mesh v24

New task → exact crystal? → promoted deterministic rule? → fast brain → deterministic judge → deep fallback if needed → signed lesson → crystallizer → distributed reconciliation.

Networking: known/LAN → AutoNAT → multi-relay reservation → DCUtR direct upgrade → encrypted relay fallback → signed Drop/store-carry-forward.

The system does not claim arbitrary NATs can always be punched. When live networking is impossible, the cold path preserves eventual convergence. Crystals are signed data, never executable code; any validated contradiction quarantines the class rule.
