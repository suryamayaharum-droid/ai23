name: HARUM Organism Pulse v2

on:
  schedule:
    - cron: '17 */3 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: harum-organism-pulse-v2
  cancel-in-progress: false

jobs:
  pulse:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout HARUM organism DNA
        uses: actions/checkout@v4
        with:
          ref: harum-archive
          fetch-depth: 0

      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Restore organism memory
        uses: actions/cache@v4
        with:
          path: runtime
          key: harum-organism-v2-${{ runner.os }}-${{ github.run_id }}
          restore-keys: |
            harum-organism-v2-${{ runner.os }}-
            harum-organism-${{ runner.os }}-

      - name: Validate organism DNA
        run: |
          for f in ecosystem/swarm/config/*.json; do
            python -m json.tool "$f" >/dev/null
          done
          python -m py_compile ecosystem/swarm/*.py

      - name: Execute organism cycle v2
        run: |
          python ecosystem/swarm/organism_cycle_v2.py             --db runtime/harum_organism.db             --output runtime/organism-v2.json

      - name: Regression tests
        run: python -m unittest discover -s ecosystem/swarm/tests -v

      - name: Persist compact organism state
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ecosystem/runtime
          cp runtime/organism-v2.json ecosystem/runtime/organism-v2-latest.json
          if [ -f runtime/journal/snapshot.json ]; then
            cp runtime/journal/snapshot.json ecosystem/runtime/journal-snapshot-latest.json
          fi
          git config user.name 'harum-organism-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add ecosystem/runtime/
          if git diff --cached --quiet; then
            echo 'No persistent state changes.'
            exit 0
          fi
          git commit -m 'Update HARUM organism v2 pulse snapshot'
          git push origin HEAD:harum-archive

      - name: Preserve short-lived telemetry
        uses: actions/upload-artifact@v4
        with:
          name: harum-organism-v2-${{ github.run_id }}
          path: |
            runtime/organism-v2.json
            runtime/journal/
          retention-days: 2
          if-no-files-found: error
